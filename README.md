# React JSON Server API

Mock REST API using JSON Server for frontend development.

## Run
npx json-server --watch db.json --port 3000

## Endpoints
GET /users  
POST /users  
PUT /users/:id  
DELETE /users/:id
